CREATE TABLESPACE myts DATAFILE  'C:\app\MIT\product\21c\oradata\XE\myts.dbf' SIZE 100M AUTOEXTEND ON NEXT 5M;

ALTER SESSION SET "_ORACLE_SCRIPT"=true;   //12버전이후 이름 앞에 "C##"을 붙이는 걸 해제

CREATE USER EXAM IDENTIFIED BY 1234
DEFAULT TABLESPACE MYTS
TEMPORARY TABLESPACE TEMP;

CREATE TABLE ORDERS (
           ORDER_ID	    NUMBER(12,0),
           ORDER_DATE   DATE ,
           ORDER_MODE	  VARCHAR2(8 BYTE),
           CUSTOMER_ID         NUMBER(6,0),
           ORDER_STATUS      NUMBER(2,0) DEFAULT 0,
           ORDER_TOTAL  	NUMBER(8,2),
           SALES_REP_ID  	NUMBER(6,0),
           PROMOTION_ID     NUMBER(6,0),
           CONSTRAINT PK_ORDER PRIMARY KEY (ORDER_ID),
           CONSTRAINT CK_ORDER_MODE CHECK (ORDER_MODE in ('direct','online'))
           ); 